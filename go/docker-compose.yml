services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: go-app
    networks:
      - private
      - public
    ports:
      - "4001:4001"

networks:
  private:
    name: private-network
    external: true
  public:
    name: public-network
    external: true
